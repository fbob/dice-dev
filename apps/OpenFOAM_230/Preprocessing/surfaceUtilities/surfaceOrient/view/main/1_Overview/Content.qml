import DICE.App 1.0

Body {
    Card {
        Title { text: app.name }
        Subheader { text: "Description" }
        BodyText {
            text: "Set normal consistent with respect to a user provided ’outside’ point. If the -inside is used the point is considered inside. "
        }
    }
}
